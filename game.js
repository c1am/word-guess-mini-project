start-button.addEventListener("click", startGame {

}

// add an event listener for click of the start button to start a "start-game" function
    // When Clicking the Start button, change the display of timer-count and main board

    // target the container with the timer
        // start the countdown from 10 with a timer-start function

    // target the main game
        // reset the main game to all blanks

// timer start function

        // use setInterval to call the function
                //check if the time is done
                    // call "game over" function
                // else
                    // check if the word is complete
                        // call "game won"
                    // else deduct the time count

// show blanks (game round)

// add event listener for reset button to call the "reset-game" function

// add keydown event listener to keydown document

// For loop for each game

    // change the score depending on win or loss